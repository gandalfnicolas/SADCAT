# Average word embeddings for seed dictionaries (person context)

Average seed embeddings for SADCAT dictionaries using person-context
seed text.

## Usage

``` r
Seed_Vectors_personcontext_Avg
```

## Format

A data frame with columns `Dictionary`, `SBERT_1`...`SBERT_768`, and
`Gemini_1`...`Gemini_768`.
