# Resolve JAVA_HOME from explicit path, env vars, PATH, or common install paths

Resolve JAVA_HOME from explicit path, env vars, PATH, or common install
paths

## Usage

``` r
.resolve_java_home(java_home = NULL)
```

## Arguments

- java_home:

  Optional explicit JAVA_HOME-like directory

## Value

Resolved JAVA_HOME directory
