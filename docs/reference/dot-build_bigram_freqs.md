# Build bigram frequency table from a corpus of text values

Build bigram frequency table from a corpus of text values

## Usage

``` r
.build_bigram_freqs(corpus)
```

## Arguments

- corpus:

  Character vector of text values (e.g., all unique responses)

## Value

An environment mapping "word1 word2" (tab-separated) keys to integer
counts
