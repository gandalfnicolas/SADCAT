# Prepare character input for quanteda tokenization with stable docnames

Prepare character input for quanteda tokenization with stable docnames

## Usage

``` r
.prepare_quanteda_text(x, prefix = "doc")
```

## Arguments

- x:

  A character vector

- prefix:

  Prefix for generated docnames

## Value

Character vector with explicit unique names for quanteda
