% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_top_dictionary_matches.R
\name{get_top_dictionary_matches}
\alias{get_top_dictionary_matches}
\title{Get Top SADCAT Dictionary Matches}
\usage{
get_top_dictionary_matches(
  data,
  text_col = "tv3",
  top_n = 20L,
  sadcat_dict = NULL,
  dictionary_names = NULL,
  min_count = 1L,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame containing text responses.}

\item{text_col}{Column containing text to inspect (default \code{"tv3"}).}

\item{top_n}{Number of top words to return per dimension (default \code{20}).}

\item{sadcat_dict}{Optional SADCAT dictionary object from
\code{\link{prepare_sadcat_dictionaries}}. If \code{NULL}, it is prepared
automatically.}

\item{dictionary_names}{Optional vector of dictionary names to include (e.g.,
\code{c("Warmth_dic", "Competence_dic")}). If \code{NULL}, uses base
SADCAT dimensions (\code{*_dic}) only.}

\item{min_count}{Minimum token frequency to keep (default \code{1}).}

\item{verbose}{Print progress? (default \code{TRUE}).}
}
\value{
A data.frame with columns:
\itemize{
  \item \code{Dictionary}: SADCAT dimension label (without \code{_dic})
  \item \code{word}: Matched token
  \item \code{n}: Token frequency in the dataset
  \item \code{prop}: Proportion within that dictionary's matched tokens
}
Returns an empty data frame with these columns if no matches are found.
}
\description{
Inspect the most frequent words in \code{text_col} that match
each SADCAT dictionary dimension. Useful for quick sanity checks of what the
dictionary coding is capturing in your dataset.
}
