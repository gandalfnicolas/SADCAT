% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_seed_similarities.R
\name{compute_seed_similarities}
\alias{compute_seed_similarities}
\title{Compute Seed Similarities}
\usage{
compute_seed_similarities(
  data,
  embedding_prefix = "SBERT",
  seed_vectors = Seed_Vectors_Avg,
  method = "correlation",
  response_col = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame with embedding columns (e.g., SBERT_1:SBERT_768)}

\item{embedding_prefix}{Prefix (or vector of prefixes) identifying embedding
columns (default "SBERT"), e.g., \code{c("SBERT", "Gemini")}.}

\item{seed_vectors}{Seed vector data. Default: Seed_Vectors_Avg}

\item{method}{"correlation" (default, uses cor()) or "cosine" (uses lsa::cosine())}

\item{response_col}{Column used for NA-gating (default: same as text_col, where
text_col is inferred as the embedding source column). Only needed if your
NA-indicator column differs from the embedding source.}

\item{verbose}{Print progress? (default TRUE)}
}
\value{
The input data with new columns: \code{prefix_SeedName.seed}
}
\description{
Compute similarity between response embeddings and SADCAT seed vectors.
For each response, computes the correlation (or cosine similarity) between
its embedding vector and each SADCAT seed vector. Works for any embedding
prefix (SBERT, Gemini, etc.) as long as matching columns exist in both the
data and seed vectors.
}
